const SectionSubHeading = ({ subHeadingText }) => {
  return (
    <h2 className="text-sm text-gray-500 md:text-base dark:text-gray-400">
      {subHeadingText}
    </h2>
  );
};

export default SectionSubHeading;
